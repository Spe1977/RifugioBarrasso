---
import { navigation, site } from "@data/site";

type Props = {
  currentPath: string;
};

const { currentPath } = Astro.props;
const isActive = (href: string) =>
  href === "/" ? currentPath === "/" : currentPath.startsWith(href);
---

<header
  class="sticky top-0 z-50 border-b-4 border-majella-red bg-white/95 backdrop-blur"
>
  <div
    class="mx-auto flex h-20 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"
  >
    <a
      href="/"
      class="flex min-w-0 items-center gap-3"
      aria-label={`${site.name} home`}
    >
      <img
        src="/assets/brand/logo.png"
        alt=""
        class="h-14 w-14 shrink-0 object-contain"
        width="56"
        height="56"
      />
      <span class="min-w-0">
        <span
          class="block truncate font-display text-sm font-black uppercase tracking-wide text-majella-green sm:text-lg"
        >
          {site.name}
        </span>
        <span
          class="block text-xs font-bold uppercase tracking-widest text-majella-wood"
        >
          Maiella - {site.altitude}
        </span>
      </span>
    </a>

    <nav
      class="hidden items-center gap-5 lg:flex"
      aria-label="Navigazione principale"
    >
      {
        navigation.map((item) => (
          <a
            href={item.href}
            class:list={[
              "border-b-2 py-2 text-xs font-bold uppercase tracking-widest transition-colors",
              isActive(item.href)
                ? "border-majella-red text-majella-green"
                : "border-transparent text-gray-500 hover:text-majella-green",
            ]}
          >
            {item.label}
          </a>
        ))
      }
    </nav>

    <button
      class="inline-flex h-11 w-11 items-center justify-center rounded-full border border-majella-green/20 text-majella-green lg:hidden"
      type="button"
      aria-label="Apri menu"
      aria-expanded="false"
      data-menu-toggle
    >
      <svg aria-hidden="true" viewBox="0 0 24 24" class="h-6 w-6" fill="none">
        <path
          d="M4 7h16M4 12h16M4 17h16"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"></path>
      </svg>
    </button>
  </div>

  <nav
    class="hidden border-t border-majella-green/10 bg-white px-4 py-3 lg:hidden"
    data-mobile-menu
  >
    <div class="mx-auto grid max-w-7xl gap-1">
      {
        navigation.map((item) => (
          <a
            href={item.href}
            class:list={[
              "rounded-md px-4 py-3 text-sm font-bold uppercase tracking-widest",
              isActive(item.href)
                ? "bg-majella-lightgreen text-majella-green"
                : "text-gray-600 hover:bg-majella-stone",
            ]}
          >
            {item.label}
          </a>
        ))
      }
    </div>
  </nav>

  <script>
    const toggle = document.querySelector("[data-menu-toggle]");
    const menu = document.querySelector("[data-mobile-menu]");

    toggle?.addEventListener("click", () => {
      const isOpen = toggle.getAttribute("aria-expanded") === "true";
      toggle.setAttribute("aria-expanded", String(!isOpen));
      toggle.setAttribute("aria-label", isOpen ? "Apri menu" : "Chiudi menu");
      menu?.classList.toggle("hidden", isOpen);
    });
  </script>
</header>
